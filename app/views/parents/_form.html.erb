<%= nested_form_for(@parent) do |f| %>
  <% if @parent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parent.errors.count, "error") %> prohibited this parent from being saved:</h2>

      <ul>
      <% @parent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :parent_name %><br>
    <%= f.text_field :parent_name %>
  </div>

  <div class="fields_for_children">
    <%= f.fields_for :children  do |child_form| %>
     <%= render 'child_fields', f: child_form %>
    <% end %>
    <br />
    <div id="code_destination">More child fields to go here</div>
    <br />
  </div>
<p id='my_parent'><%= link_to "link for adding the children", '#' %></p> 

 
   <br />
    <%= f.submit %>
  </div>
<% end %>









<

